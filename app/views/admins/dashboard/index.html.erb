<h1>Admin</h1>
  <table>
    <tr>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Email</th>
      <th>Balance</th>
      <th>Status</th>
    </tr>
    <% @users.each do |user| %>
    <tr>
      <td><%= user.first_name %></td>
      <td><%= user.last_name %></td>
      <td><%= user.email %></td>
      <td>balance</td>
      <td>status</td>
    </tr>
    <% end %>
  </table>

  <%= link_to "Create New User", admins_dashboard_user_new_path %>
